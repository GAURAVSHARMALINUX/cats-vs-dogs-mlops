[flake8]
max-line-length = 100
max-complexity = 12
exclude = .git,__pycache__,.venv,venv,mlruns,data,build,dist
extend-ignore = E203, W503, E402

[tool:pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
