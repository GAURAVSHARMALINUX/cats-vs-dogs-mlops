# Runtime dependencies for the inference container only (M2).
# Deliberately smaller than requirements.txt: no mlflow, dvc or matplotlib.
tensorflow-cpu==2.20.0
keras==3.15.1
numpy==2.4.6
pillow==12.3.0
fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
prometheus-fastapi-instrumentator==8.1.0
pyyaml==6.0.3
