apiVersion: v1
kind: Service
metadata:
  name: cats-vs-dogs-api
  namespace: mlops
  labels:
    app: cats-vs-dogs-api
spec:
  type: NodePort
  selector:
    app: cats-vs-dogs-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30080
